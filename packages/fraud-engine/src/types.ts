export interface RuleResult {
    score: number;
    reasons: string[];
}

export interface AiResult {
    score: number;
    reasons: string[];
}

export interface FinalResult {
    score: number;
    reasons: string[];
}
